# bug记录

## 已经解决
1. 无法生成外验记录表
2. 无法通过命令行修改模型选择，总是在加载默认的resnet18
3. 运行必须要命令行指令，无法通过读取配置文件获取默认超参数
4. 最佳超参数的记录没有记录最优结果，在下一次运行后有可能会更差的修改
5. 没有为早停功能设置关闭指令和配置

## 未解决
1. 使用分层抽样均衡划分训练集和内验集中不同标签的病人数量

## 添加功能
1. 特征融合（简易）：
    读取已经提取出来的模态特征
    每个病人模态1特征(n, m)，n是该模态图片数量，m是提取的特征维度，压缩成（求和取平均）(1, m)
    然后不同模态的concat起来成为一个整体，通过L2进行预测
2. 实现管理命令（subcommand）来调用整个项目功能，所有功能的参数都可以从默认配置文件读取